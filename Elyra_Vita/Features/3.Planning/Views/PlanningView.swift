import SwiftUI
struct PlanningView: View {
    // MARK: - Ansicht

    var body: some View {
        VStack {
            // Die Planungsinhalte werden hier spaeter ergaenzt.
        }
        .appBackground()
    }
}
